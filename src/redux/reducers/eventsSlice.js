const initialState = {
  events: [],
  archive: [],
};

const eventsReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export { eventsReducer };
